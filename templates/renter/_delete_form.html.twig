<form method="post" action="{{ path('app_renter_delete', {'id': renter.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ renter.id) }}">
    <button class="btn">Supprimer</button>
</form>
