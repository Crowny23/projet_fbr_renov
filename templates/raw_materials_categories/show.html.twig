{% extends 'base.html.twig' %}

{% block title %}RawMaterialsCategories{% endblock %}

{% block body %}
    <h1>RawMaterialsCategories</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ raw_materials_category.id }}</td>
            </tr>
            <tr>
                <th>Nom de la catégorie</th>
                <td>{{ raw_materials_category.name }}</td>
            </tr>
            <tr>
                <th>Date de création</th>
                <td>{{ raw_materials_category.createdAt ? raw_materials_category.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_raw_materials_categories_index') }}">Retour à la liste</a>

    <a href="{{ path('app_raw_materials_categories_edit', {'id': raw_materials_category.id}) }}">Modifier</a>

    {{ include('raw_materials_categories/_delete_form.html.twig') }}
{% endblock %}
