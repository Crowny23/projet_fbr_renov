{% extends 'base.html.twig' %}

{% block title %}Categories Matériaux{% endblock %}

{% block body %}
    <h1>Categories Matériaux</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom de la catégorie</th>
                <td>{{ raw_materials_category.name }}</td>
            </tr>
            <tr>
                <th>Date de création</th>
                <td>{{ raw_materials_category.createdAt ? raw_materials_category.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_raw_materials_categories_index') }}" class="btn btn-primary mb-3"><i class="fa-solid fa-reply"></i> Retour</a>

    <a href="{{ path('app_raw_materials_categories_edit', {'id': raw_materials_category.id}) }}" class="btn btn-primary mb-3">Modifier</a>

    {{ include('raw_materials_categories/_delete_form.html.twig') }}
{% endblock %}
