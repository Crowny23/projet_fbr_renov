<form method="post" action="{{ path('app_raw_materials_categories_delete', {'id': raw_materials_category.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette catégorie ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ raw_materials_category.id) }}">
    <button class="btn btn-danger"><i class="fa fa-trash-can"></i></button>
</form>
