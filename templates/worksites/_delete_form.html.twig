<form method="post" action="{{ path('app_worksites_delete', {'id': worksite.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce chantier ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ worksite.id) }}">
    <button class="btn btn-danger"><i class="fa fa-trash-can"></i></button>
</form>
