<div class="row mb-3">
    {{ form_start(form) }}
        <div class="col-sm-12">
            {{ form_widget(form) }}
        </div>
        <button class="btn btn-success mt-3 mb-2">{{ button_label|default('Enregistrer') }}</button>
    {{ form_end(form) }}
</div>
