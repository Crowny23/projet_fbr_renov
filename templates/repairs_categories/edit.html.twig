{% extends 'base.html.twig' %}

{% block title %}Dépannages | Modification de catégorie{% endblock %}

{% block body %}
    <div class="contain col-12 d-flex justify-content-between">
        <a href="{{ path('app_repairs_categories_index') }}" class="btn btn-primary ms-1 mt-1"><i class="fa-solid fa-reply"></i></a>
        {{ include('repairs_categories/_delete_form.html.twig') }}
    </div>

    <h1 class="fs-2 text-center pt-5 container col-12 mx-auto">Modifier la catégorie</h1>
    <div class="mb-3 container col-11 col-sm-9 col-xl-6 col-xxl-4 mx-auto">
        {{ include('repairs_categories/_form.html.twig', {'button_label': 'Enregistrer'}) }}
        
    </div>
    
{% endblock %}
