<form method="post" action="{{ path('app_repairs_categories_delete', {'id': repairs_category.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette catégorie ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ repairs_category.id) }}">
    <button class="btn btn-danger"><i class="fa fa-trash-can"></i></button>
</form>
