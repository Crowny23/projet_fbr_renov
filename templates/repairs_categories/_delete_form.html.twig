<form method="post" action="{{ path('app_repairs_categories_delete', {'id': repairs_category.id}) }}" onsubmit="return confirm('Êtes-vous sûr de supprimer cette catégorie ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ repairs_category.id) }}">
    <button class="btn">Supprimer</button>
</form>
