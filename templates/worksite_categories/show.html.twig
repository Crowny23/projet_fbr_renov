{% extends 'base.html.twig' %}

{% block title %}Chantiers | Détails de la catégorie{% endblock %}

{% block body %}
<div class="d-flex justify-content-center">
    <h1 class="col-12 col-sm-9 text-center fs-2">Détails de la catégorie</h1>
</div>
<table class="table">
    <tbody>
        <tr>
            <th>Nom de la catégorie</th>
            <td>{{ worksite_category.nameWorksiteCategories }}</td>
        </tr>
        <tr>
            <th>Date de création</th>
            <td>{{ worksite_category.createdAt ? worksite_category.createdAt|date('d M Y H:i:s') : '' }}</td>
        </tr>
        <tr>
            <th>Date de modification</th>
            <td>{{ worksite_category.updatedAt ? worksite_category.updatedAt|date('d M Y H:i:s') : '' }}</td>
        </tr>
    </tbody>
</table>

<div class="d-flex">
    <a href="{{ path('app_worksite_categories_index') }}" class="btn btn-primary mb-3 me-2"><i class="fa-solid fa-reply"></i> Retour</a>

    <a href="{{ path('app_worksite_categories_edit', {'id': worksite_category.id}) }}" class="btn btn-primary mb-3 me-2">Modifier</a>

    {{ include('worksite_categories/_delete_form.html.twig') }}
</div>
{% endblock %}
