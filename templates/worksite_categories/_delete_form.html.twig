<form method="post" action="{{ path('app_worksite_categories_delete', {'id': worksite_category.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette catégorie ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ worksite_category.id) }}">
    <button class="btn">Supprimer</button>
</form>
