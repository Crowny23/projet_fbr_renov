<form method="post" action="{{ path('app_worksite_images_delete', {'id': worksite_image.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette image');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ worksite_image.id) }}">
    <button class="btn btn-danger"><i class="fa fa-trash-can"></i></button>
</form>
