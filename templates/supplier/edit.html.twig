{% extends 'base.html.twig' %}

{% block title %}Commandes | Modification du fournisseur{% endblock %}

{% block body %}
    <div class="contain col-12 d-flex justify-content-between">
        <a href="{{ path('app_supplier_index') }}" class="btn btn-primary"><i class="fa-solid fa-reply"></i></a>
        {{ include('supplier/_delete_form.html.twig') }}
    </div>
    <h1 class="fs-2 text-center pt-5 container col-12 mx-auto">Modification du fournisseur</h1>
    <div class="mb-3 container col-11 col-sm-9 col-xl-6 col-xxl-4 mx-auto">
        {{ include('supplier/_form.html.twig', {'button_label': 'Enregistrer'}) }}
    </div>
{% endblock %}
