{% extends 'base.html.twig' %}

{% block title %}Modifier Customer{% endblock %}

{% block body %}
    <a href="{{ path('app_customers_index') }}" class="btn btn-primary ms-1 mt-1"><i class="fa-solid fa-reply"></i></a>
    <h1 class="mb-3 mt-2 ps-4 container col-4 mx-auto">Modifier Customer</h1>

    <div class="row mb-3 container col-4 mx-auto">
        {{ include('customers/_form.html.twig', {'button_label': 'Update'}) }}
        {{ include('customers/_delete_form.html.twig') }}
    </div>

    
{% endblock %}
