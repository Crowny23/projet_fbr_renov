{% extends 'base.html.twig' %}

{% block title %}Modifier Location{% endblock %}

{% block body %}
    <a href="{{ path('app_rentals_index') }}" class="btn btn-primary ms-1 mt-1"><i class="fa-solid fa-reply"></i></a>

    <h1 class="mb-3 mt-2 ps-4 container col-4 mx-auto">Modifier Location</h1>
    <div class="row mb-3 container col-4 mx-auto">
        <div class="row">
            {{ include('rentals/_form.html.twig', {'button_label': 'Enregistrer'}) }}
            {{ include('rentals/_delete_form.html.twig') }}
        </div>
    </div>
    

   
{% endblock %}
