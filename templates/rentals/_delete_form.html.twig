<form method="post" action="{{ path('app_rentals_delete', {'id': rental.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette location?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rental.id) }}">
    <button class="btn btn-danger"><i class="fa fa-trash-can"></i></button>
</form>
