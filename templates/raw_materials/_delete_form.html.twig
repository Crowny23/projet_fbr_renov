<form method="post" action="{{ path('app_raw_materials_delete', {'id': raw_material.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce matériau ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ raw_material.id) }}">
    <button class="btn">Supprimer</button>
</form>
