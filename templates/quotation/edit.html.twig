{% extends 'base.html.twig' %}

{% block title %}Modification de devis{% endblock %}

{% block body %}
    <h1>Modifier le devis</h1>
    <div class="row mb-3 container col-4">
        {{ include('quotation/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    </div>
    <a href="{{ path('app_quotation_index') }}">Retour</a>

    {{ include('quotation/_delete_form.html.twig') }}
{% endblock %}
