{% extends 'base.html.twig' %}

{% block title %}RawMaterialsOrdered{% endblock %}

{% block body %}
    <h1>RawMaterialsOrdered</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ raw_materials_ordered.id }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ raw_materials_ordered.quantity }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ raw_materials_ordered.createdAt ? raw_materials_ordered.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Total_price_raw_material</th>
                <td>{{ raw_materials_ordered.totalPriceRawMaterial }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_raw_materials_ordered_index') }}">Retour à liste</a>

    <a href="{{ path('app_raw_materials_ordered_edit', {'id': raw_materials_ordered.id}) }}">Modifier</a>

    {{ include('raw_materials_ordered/_delete_form.html.twig') }}
{% endblock %}
