<form method="post" action="{{ path('app_repairs_delete', {'id': repair.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce dépannage ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ repair.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
